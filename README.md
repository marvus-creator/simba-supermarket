# Simba Supermarket

A React supermarket app built with Vite.

## DevTools Audit Notes
- Opened React DevTools Components tab
- ProductCard receives: product (object), onAddToCart (function)
- Search bar causes re-render of ProductList on every keystroke
- No unnecessary re-renders found
